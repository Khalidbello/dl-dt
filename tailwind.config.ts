// tailwind.config.ts
const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        exo: ["var(--font-exo2)", "sans-serif"],
        geometric: ["var(--font-inter)", "sans-serif"],
      },
    },
  },
};

export default config;
